---
export interface Props { items: string[] }
const { items } = Astro.props;
---
{items?.length > 0 && (
  <ul class="pains" role="list" style="list-style:none;padding:0;margin:0;">
    {items.map((t) => <li class="pains__item">{t}</li>)}
  </ul>
)}
